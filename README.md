# ScheduleSolve

This is intended to become a solver for schedules under constraints. Currently, it just uses a version of Dijkstra's algorithm to find shortest paths on a graph, and makes pretty pictures with the Dot language. 
